/**
 * 
 */
package easyserver.protocol;

import easyserver.core.AbstractComponent;

/**
 * @author cango
 *
 */
public class AbstractProtocolHandler extends AbstractComponent implements ProtocolHandler {

	/* (non-Javadoc)
	 * @see easyserver.protocol.ProtocolHandler#invoke(easyserver.protocol.Protocol)
	 */
	public void invoke(Protocol protocol) {

	}

	@Override
	protected void initComponent() {
		
	}

}
